version: '3.8'

services:
  app1:
    build: ./backend
    environment:
      - DATABASE_URL=mysql+pymysql://root:F6l8cwIxp3Cb@127.0.0.1:3306/cloudoblig
      - SECRET_KEY=hamburgerdressing
      - REDIS_URL=redis://localhost:6379
    ports:
      - "8000:8000"

  app2:
    build: ./backend
    environment:
      - DATABASE_URL=mysql+pymysql://root:F6l8cwIxp3Cb@127.0.0.1:3306/cloudoblig
      - SECRET_KEY=hamburgerdressing
      - REDIS_URL=redis://localhost:6379
    ports:
      - "8000:8000"

  app3:
    build: ./backend
    environment:
      - DATABASE_URL=mysql+pymysql://root:F6l8cwIxp3Cb@127.0.0.1:3306/cloudoblig
      - SECRET_KEY=hamburgerdressing
      - REDIS_URL=redis://localhost:6379
    ports:
      - "8000:8000"

  redis:
    image: redis:7
    container_name: redis

  nginx:
    image: nginx:latest
    container_name: nginx
    ports:
      - "8080:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app1
      - app2
      - app3

  # frontend:
  #   build:
  #     context: ./frontend
  #   container_name: frontend
  #   ports:
  #     - "3000:80"
  #   depends_on:
  #     - nginx
